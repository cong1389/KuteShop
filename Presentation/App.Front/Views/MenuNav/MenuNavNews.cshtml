<!----MenuNavNews-->
@using App.Core.Extensions
@model IEnumerable<App.FakeEntity.Menus.MenuNavViewModel>

@if (Model.IsAny())
{
    <div id="categories-3" class="widget sidebar-widget widget_categories">
        <div class="sidebar-title">
            <h3>@T("DanhMuc")</h3>
        </div>
        <ul>
            @foreach (var item in Model)
            {
                <li class="cat-item cat-item-15">
                    <a href="@Url.Action("GetContent", "Menu", new {menu = item.SeoUrl, area = ""})">@item.MenuName</a>
                </li>
            }
        </ul>
    </div>

    @*<div class="block left-module">
            <p class="title_block">@T("DanhMuc")</p>
            <div class="block_content">
                <!-- layered -->
                <div class="layered layered-category">
                    <div class="layered-content">
                        <ul class="tree-menu">
                            @foreach (var item in Model)
                            {
                                <li><span></span><a href="@Url.Action("GetContent", "Menu", new {menu = item.SeoUrl, area = ""})">@item.MenuName</a></li>
                            }
                        </ul>
                    </div>
                </div>
                <!-- ./layered -->
            </div>
        </div>*@



}

<!----/MenuNavNews-->