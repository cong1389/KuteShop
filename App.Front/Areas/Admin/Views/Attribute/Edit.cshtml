@using Resources
@model App.FakeEntity.Attribute.AttributeViewModel
@{
    ViewBag.Title = FormUI.Edit;
    ViewBag.Toolbar = new[] { "Save", "Divider", "Back" };
}
@using (Html.BeginForm("Edit", "Attribute", FormMethod.Post, new { data_container = ViewContext.RouteData.Values["controller"], enctype = "multipart/form-data" }))
{
    @Html.AntiForgeryToken()
    @Html.HiddenFor(x => x.Id)
	Html.RenderPartial("_CreateOrUpdate", Model);

}



